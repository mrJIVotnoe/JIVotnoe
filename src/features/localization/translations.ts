import { translations as originalTranslations } from '../../translations';
// Re-exporting to maintain backward compatibility during migration if needed, 
// but ideally we just move the content here.
export const translations = originalTranslations;